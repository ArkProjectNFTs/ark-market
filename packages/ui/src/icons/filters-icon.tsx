import type { PropsWithClassName } from "..";
import { Icon } from "./icon";

export default function FiltersIcon({ className }: PropsWithClassName) {
  return <Icon className={className} icon="E" />;
}
