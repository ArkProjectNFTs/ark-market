import type { PropsWithClassName } from "..";
import { Icon } from "./icon";

export default function CopyIcon({ className }: PropsWithClassName) {
  return <Icon className={className} icon="F" />;
}
