import type { PropsWithClassName } from "..";
import { Icon } from "./icon";

export default function EthereumLogo2({ className }: PropsWithClassName) {
  return <Icon className={className} icon="X" />;
}
