import type { PropsWithClassName } from "..";
import { Icon } from "./icon";

export default function DiscordIcon({ className }: PropsWithClassName) {
  return <Icon className={className} icon="c" />;
}
