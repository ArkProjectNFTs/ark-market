import type { PropsWithClassName } from "..";
import { Icon } from "./icon";

export default function StarknetLogo2({ className }: PropsWithClassName) {
  return (
    <Icon className={className} icon="Y"/>
  );
}
