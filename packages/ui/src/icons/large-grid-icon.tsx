import type { PropsWithClassName } from "..";
import { Icon } from "./icon";

export default function LargeGridIcon({ className }: PropsWithClassName) {
  return <Icon className={className} icon="B" />;
}
