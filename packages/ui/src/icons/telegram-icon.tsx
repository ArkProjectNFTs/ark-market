import type { PropsWithClassName } from "..";
import { Icon } from "./icon";

export default function TelegramIcon({ className }: PropsWithClassName) {
  return <Icon className={className} icon="U" />;
}
