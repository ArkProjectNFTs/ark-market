import type { PropsWithClassName } from "..";
import { Icon } from "./icon";

export default function WalletIcon({ className }: PropsWithClassName) {
  return <Icon className={className} icon="O" />;
}
